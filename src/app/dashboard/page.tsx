import { Button } from "@heroui/button";
import { type FC } from 'react';

const Dashboard: FC = () => {
  return (
    <div className="flex justify-center items-center h-screen m-screen">
      <Button color="primary">控制台</Button>
    </div>
  )
}
export default Dashboard;